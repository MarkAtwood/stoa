[package]
name = "stoa-sieve-native"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
license = "MIT"

[dependencies]
fancy-regex = "0.13"
tracing = "0.1"

[dev-dependencies]
stoa-sieve = { path = "../sieve" }
